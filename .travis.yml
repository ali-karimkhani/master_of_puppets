dist: trusty
sudo: false

language: go
go:
 - 1.5.3

branches:
  only:
    - master

before_script:
  - echo "before"
  - mkdir $GOPATH/src/sample ; cp src/sample.go $GOPATH/src/sample

script:
  - go build sample

after_script:
  - /home/go_app/bin/sample &
